/**
 * Cores da marca em hex puro, pra uso em `react-konva` — o canvas 2D não
 * entende `var(--token)` do CSS, só aceita cor resolvida.
 */
export const CORES = {
  primario: "#1C8A4B",
  perigo: "#B03A3A",
  card: "#FFFFFF",
  tinta: "#14140F",
  folha: "#F7F4EE",
} as const;
